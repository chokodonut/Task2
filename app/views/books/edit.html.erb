<!DOCTYPE HTML>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="books.scss">
    <title>WedCampTask2</title>
<body>
    
  <h1>Editing Book</h1>
   <%= form_with model: @book,url: edit_book_path(@book.id), method: :patch do |f| %>
   
   <%= f.text_field :title %>
   <%= f.text_area :body %>
   
   <%= f.submit 'Update Book' %>
   <!--<%= link_to book_path(@book.id) %>-->
   
   <a class="show" href="/book">Show</a>
   <a class="back" href="books">Back</a>
  <% end %>
 </body>
 
</html>